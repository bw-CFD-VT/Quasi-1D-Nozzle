#include "Norms.hpp"

using namespace std;

void Norms(int counter,double imax,vector<vector<vector<double> > > Residual, vector<vector<vector<double> > > &L2, 
            vector<vector<vector<double> > > &L1, vector<vector<vector<double> > > &Linf)
{

L1.resize(counter+1);
L1[counter].resize(imax);

L2.resize(counter+1);
L2[counter].resize(imax);

Linf.resize(counter+1);
Linf[counter].resize(imax);







return;

}